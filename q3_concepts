姓名：周文韋
學號：c112181106

Q1：請說明管線符號 | 的功能，並舉一個實際使用的例子。
A1：管線符號（Pipe）的作用是將前一個指令的 「標準輸出 (stdout)」 直接導向成為後一個指令的 「標準輸入 (stdin)」。它像是一條傳送帶，讓你不需要產生中間暫存檔就能串聯多個工具。

Q2：請說明 > 和 >> 的差異，各舉一個使用情境。
A2：(>)差異：會清空目標檔案的舊內容，並寫入新的資料。如果檔案不存在則會建立新檔。

情境：更新系統狀態快照。例如每天產出一份全新的當前進程清單

(>>)差異：保留檔案原有內容，將新資料接在檔案的最後一行之後。

情境：紀錄日誌 (Log)。例如持續記錄某個程式的運行時間點

Q3：請寫出一行指令，找出 /etc/passwd 中有多少個使用者的 shell 是 /bin/bash。
    提示：可以結合 grep 和 wc 指令。
A3：grep "/bin/bash" /etc/passwd | wc -l